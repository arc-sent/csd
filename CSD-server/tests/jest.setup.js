// Тесты не должны трогать настоящий SMTP, даже если он настроен в .env для
// локальной разработки (см. shared/mailer.js) — иначе тесты реально шлют
// письма и виснут на сетевых таймаутах вместо мгновенного дев-режима.
// dotenv.config(), которым каждый тестовый файл читает .env, по умолчанию не
// перезаписывает уже выставленные process.env — эти пустые значения
// побеждают.
process.env.SMTP_HOST = '';
process.env.SMTP_USER = '';
