// Адаптер над shared/chess-rules.js (общий движок правил с сервером и
// публичным тренажёром — НЕ копируется). Сам файл подключается классическим
// <script> в index.html (см. комментарий там), а не через import: это UMD-
// модуль, и при импорте его как ES-модуля сборщик production-бандла (Rollup)
// принимает UMD-обёртку за CommonJS и не выставляет window.Admin. Здесь
// только читаем то, что классический скрипт уже положил в window к моменту
// выполнения этого модуля (классические скрипты выполняются раньше любых
// module-скриптов, включая этот).
export const chessRules = window.Admin.chessRules;
